import React from "react";

function App() {
  return (
    <body className="page">
      function puzzle => (){" "}
      {console.log(("b" + "a" + +"a" + "a").toLowerCase())};
    </body>
  );
}

export default App;
